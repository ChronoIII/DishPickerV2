import 'package:flutter/material.dart';

class HomeDefaultWidget extends StatelessWidget {
  const HomeDefaultWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('Touch me daddy.');
  }
}
